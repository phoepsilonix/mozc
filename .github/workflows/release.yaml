name: Release

on:
  create:
    tags:
      - 'v*'
      - 'commit-*'

jobs:
  release:
    name: Release
    runs-on: ubuntu-latest
    container:
      image: debian:bookworm
    steps:
      - uses: actions/checkout@v4
      - name: install build tools (Linux)
        id: startup
        run: |
          echo "container_name=debian-bookworm" > $GITHUB_OUTPUT
          apt-get update
          apt-get install -y sudo apt-utils wget
          sudo sed 's/Types: deb *$/Types: deb deb-src/' -i /etc/apt/sources.list.d/debian.sources
          sudo apt-get update
          sudo apt-get build-dep -y mozc
          sudo apt-get install -y build-essential dpkg-dev make git qt6ct qt6-base-dev libfcitx5-qt6-dev curl unzip
      - name: Mount bazel cache
        uses: actions/cache@v4
        with:
          path: "/home/runner/.cache/bazel"
          key: bazel-${{ runner.os }}-mozc-${{ steps.startup.outputs.container_name }}
          restore-keys: bazel-${{ runner.os }}-mozc-${{ steps.startup.outputs.container_name }}
      - name: Mount rustup cache
        uses: actions/cache@v4
        with:
          path: "/home/runner/.rustup"
          key: rustup-${{ runner.os }}-mozc-${{ steps.startup.outputs.container_name }}
          restore-keys: rustup-${{ runner.os }}-mozc-${{ steps.startup.outputs.container_name }}

      - name: Install bazel
        run: |
           curl -LO https://github.com/bazelbuild/bazel/releases/download/7.1.1/bazel-7.1.1-installer-linux-x86_64.sh
           bash bazel-7.1.1-installer-linux-x86_64.sh --user
      - name: Install mold linker
        uses: rui314/setup-mold@staging
        with:
          mold-version: 2.30.0
          make-default: true
      - uses: actions/checkout@v4
        with:
          ref: debian
          submodules: 'recursive'
          path: source
      - name: Setup variables
        id: variables
        shell: bash
        run: |
          cd source
          TAG=${GITHUB_REF##*/}
          if [[ "$TAG" =~ ^commit- ]]; then
            TAG=${TAG#commit-}
            cd with-jp-dict
            git fetch --prune --unshallow --tags --no-recurse-submodules --filter=blob:none
            git tag ${GITHUB_REF##*/} -d
            git push origin ${GITHUB_REF##*/} -d
          fi
          echo ${TAG}
          echo "tag_name=$(git describe --tags --always ${TAG})" > $GITHUB_OUTPUT
          echo "tag_name=$(git describe --tags --always ${TAG})"
      - name: Build
        shell: bash
        run: |
          export PATH=$HOME/bin:$PATH
          source ~/.bazel/bin/bazel-complete.bash
          cd source
          sed "s/^\(LDFLAGS:=.*\)/\1 -fuse-ld=mold/" debian/rules -i
          dpkg-buildpackage -b --no-sign
          cd ..
          mkdir -p release
          mv *deb mozc_* release/
      - name: Create release
        uses: ncipollo/release-action@v1
        with:
          name: ${{ steps.variables.outputs.tag_name }}
          omitBody: true
          artifacts: 'release/*'
          token: ${{ secrets.GITHUB_TOKEN }}

